# KXBTC15M ML-side + mechanical average-down runner. These dependencies exclude
# Prophet and yfinance; the live ML path uses raw Coinbase candles only.
kalshi-python-async>=3.22,<4
aiohttp>=3.9
cryptography>=42.0
tzdata>=2024.1
pandas>=2.0
numpy>=1.26
scikit-learn>=1.5,<2
scipy>=1.10
joblib>=1.3
# The SDK's current wheel imports urllib3 but does not declare it transitively.
urllib3>=2,<3
