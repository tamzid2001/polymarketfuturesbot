# KXBTC15M ML-side + mechanical average-down runner.  The stored ML model uses
# the same pre-open feature construction as the validated ML inference runner.
-r requirements_kalshi.txt
# The SDK's current wheel imports urllib3 but does not declare it transitively.
urllib3>=2,<3
