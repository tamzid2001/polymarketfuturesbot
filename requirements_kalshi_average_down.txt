# KXBTC15M ML-side + mechanical average-down runner. The stored model uses
# raw candles and settled outcomes only; it does not fit or consume Prophet.
-r requirements_kalshi.txt
# The SDK's current wheel imports urllib3 but does not declare it transitively.
urllib3>=2,<3
