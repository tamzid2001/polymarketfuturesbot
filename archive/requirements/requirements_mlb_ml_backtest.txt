# Isolated dependency set for the historical MLB research pipeline.  The live
# Polymarket monitor continues to use requirements.txt and does not import
# scikit-learn unless ML mode is explicitly enabled.
-r requirements.txt
numpy>=2.0,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2
